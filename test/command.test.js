var Command = require('../lib/command')

module.exports = {
  /*testShowHelpWithNoParams: function(test){
        var cmd = new Command();
        var fakeArgs = ['node', 'foobar'];
        cmd.run(fakeArgs);
        test.ok(true); // TODO
        test.done();
    }*/
}
